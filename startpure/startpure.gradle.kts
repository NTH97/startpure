version = "1.0.0"

project.extra["PluginName"] = "Start Pure"
project.extra["PluginDescription"] = "Automated pure account starter"

tasks {
    jar {
        archiveBaseName.set("StartPure")
    }
}
